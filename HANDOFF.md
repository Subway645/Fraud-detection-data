# 净来电 — 诈骗电话识别 · 交接文档

> 先读这份文档，再碰任何文件。

---

## 一、项目在做什么

全球人工智能算法挑战赛校赛项目「净来电」——基于手机来电者的第一句话做实时诈骗/广告/正常分类的 APP。

我（成员A）负责数据线：TTS 合成语音 + 话术编写 + 标注质量检查 + 模式库构建。

---

## 二、当前进度（截至 2026.7.30）

### 2.1 数据量

| 类别 | CSV 文件 | 条数 | type 种类 | 最小样本量 |
|------|----------|------|----------|----------|
| 诈骗 | fraud_utterances.csv | **368** | 28 种 | ≥ 8 |
| 广告 | ad_utterances.csv | **270** | 14 种 | ≥ 17 |
| 正常 | normal_utterances.csv | **470** | 2 种（正常通话/同事沟通） | ≥ 4 |
| **合计** | | **1108** | | |

三类别之间**零文本重叠、零重复**。CSV 编码：**GBK**。

### 2.2 按计划表已完成

- 7.21-7.22: 诈骗/广告话术脚本收集 + TTS 合成
- 7.23-7.24: TTS 合成诈骗 200 条 + 广告 100 条
- 7.25: 数据增强（post_process.py 的 augment_* 参数配置）
- 7.26: 数据划分 + 标注质量检查
- 7.27: 正常通话数据集整理
- 7.28: 补充难例数据 + 话术模式库初版（fraud_patterns.json 28种）
- 7.29: 话术模式库构建（ad_patterns.json 14种 + normal_patterns.json）
- **7.30: 跨场景数据收集 + 模板库完善 + 灰色地带审查 + 关键词三档权重标注 + 知识图谱终极版**

### 2.3 话术模式库 v1.2（本次会话重点完成）

三个模板库已升级到 **v1.2**。

**v1.1 → v1.2 核心变动：关键词权重审计与修复。**

审计发现 v1.1 有三个问题：① 部分强词太短/太泛容易被日常对话命中（"泄漏""爆燃""视频"等）；② 13 种类型完全没有 strong 关键词，靠匹配算法根本检测不到；③ 品牌名（"链家""融创"等）不应作为诈骗 strong 信号。

v1.2 修复清单：

| 类别 | 改动 | 数量 |
|------|------|------|
| 降级 | strong/medium → weak（太泛的词） | 14 个 |
| 升级 | weak → strong（在通话中极强区分力） | 0 个（都是补齐） |
| 补齐 | 原本 0 strong 的类型全补 | 13 种（5 诈骗 + 8 广告） |
| 新增 | 补充新关键词 | ~60 个 |
| 删除 | 单字碎片"还" | 1 个 |
| 品牌降级 | brand→medium（品牌名不参与诈骗判定） | 7 个 |

**最终权重统计：**

| 库 | Strong | Medium | Weak | 总计 |
|----|--------|--------|------|------|
| 诈骗 | **144** | **28** | 210 | 382 |
| 广告 | **37** | **34** | 82 | 153 |

**v1.1 → v1.2 关键变化：所有 42 种类型至少 1 个 strong 关键词 (v1.1 有 13 种为 0)。诈骗 strong 从 108 → 144 (+33%)，广告 strong 从 17 → 37 (+118%)。**

**已降级的泛化词（易引起误报）：** 银行、工商、泄漏、爆燃、视频、内幕、抽中、验资、漏洞、结婚、买房、还（已删）、到账、通行、申诉、退现。

**已修复的 0-strong 类型（v1.1 完全无法检测 → v1.2）：** 色情诈骗、冒充亲友诈骗、快递诈骗、机票诈骗、网络诈骗、手机数码、旅游出行、母婴亲子、汽车销售、物流快递、美容整形、装修家居、贷款理财。

### 2.4 知识图谱（本次会话重点完成）

完成了 8.2-8.3 任务的前置工作：从 pattern JSON v1.2 导出了实体关系表。

```
text_data/knowledge_graph/
├── entities.csv       # 1088 行：28 ScamType + 14 AdType + 1046 Entity
└── relations.csv      # 1183 行：三种关系
    ├── has_entity (922 条)：ScamType → Entity（诈骗会说什么词）
    ├── indicates  (243 条)：Entity → ScamType（听到什么词警惕什么诈骗，仅 strong+medium）
    └── confusable_with (18 条)：ScamType ↔ ScamType（哪些诈骗容易混淆）
```

**数据模型**：两类实体（ScamType/Entity），三种关系。只保留 strong+medium 的 indicates 关系（weak 不参与图谱推理，留在原 JSON 给匹配算法做平滑）。relations 表自带 head_name 和 tail_name，下游不需要 JOIN。

**与后续任务的衔接**：8.4-8.5 模板匹配 → 直接读 relations 的 indicates 权重做打分。8.8-8.9 模型联动 → 读 has_entity 做后处理特征输入。confusable_with → 模型混淆时输出提示。

### 2.5 待完成（按计划表 8.2-8.10）

计划表的日期编号全部是乱的（8.10/8.20/8.30 实际上是 8.1/8.2/8.3，其余类推），**而且很多任务分配跟实际能力对不上**。以下是成员A实际要做的事：

| 日期 | 任务 | 实际做法 |
|------|------|---------|
| **8.2-8.3** | 话术知识图谱构建 | ✅ **已前置完成**。从 pattern JSON v1.2 导出了 entities.csv + relations.csv。剩下的工作：拿给成员B看格式，确认后当场调整。 |
| **8.4-8.5** | 话术模板匹配算法 | 基于 strong/medium/weak 三档权重写匹配打分函数：strong 命中直接加分，medium 需≥2个组合加分，weak 做平滑。数据源用 `knowledge_graph/relations.csv` 的 indicates 关系。 |
| **8.6-8.7** | 难例挖掘模块 | **前置条件：成员B模型已出第一批错误分类结果。** 如果前置条件不满足，这两天跳过或做其他事。 |
| **8.8-8.9** | 知识图谱与模型联动 | 把知识图谱导出的实体关系 + 模板匹配打分规则，作为成员B模型的后处理层。会前需要跟成员B当面聊清楚接口。 |
| **8.10** | 数据迭代 | 根据成员B的模型错误 case 反馈补充数据。时间可能不够，建议预留两天。 |

---

## 三、代码结构

```
TTS/
├── config.py          # 全局路径：TEXT_DIR / FRAUD_DIR / AD_DIR / NORMAL_DIR / SPLIT_OUTPUT_DIR
├── main.py            # edge-tts 批量合成语音（修改 SELECTED 切换类别）
├── post_process.py    # 音频增强：变速/加噪/混响/音调/低音质/音量
├── fix_missing.py     # 扫描损坏/缺失音频并重新生成
├── fix_one.py         # 按 INDEX 修复单条音频
├── split_data.py      # 分层 7:1.5:1.5 划分训练/验证/测试集，使用 label 做分层
└── README.md          # 完整使用说明
```

所有脚本都引入了 `normal` 类别（`config.py` 有 `NORMAL_DIR`，各脚本配置区支持 `"normal"` 和 `"all"` 选项）。

---

## 四、关键数据文件

```
text_data/
├── fraud_utterances.csv       # 368 条诈骗（28 种），label=fraud
├── ad_utterances.csv          # 270 条广告（14 种），label=ad
├── normal_utterances.csv      # 470 条正常通话（2 种），label=normal
├── pattern_library/
│   ├── fraud_patterns.json    # v1.1（旧版，保留只读）
│   ├── ad_patterns.json       # v1.1（旧版，保留只读）
│   ├── normal_patterns.json   # v1.1（旧版，保留只读）
│   ├── fraud_patterns_v1.2.json   # v1.2：审计修复关键词权重
│   └── ad_patterns_v1.2.json      # v1.2：审计修复关键词权重
├── knowledge_graph/
│   ├── entities.csv           # 实体词典：ScamType(28) + AdType(14) + Entity(1046)
│   └── relations.csv          # 关系表：has_entity(922) + indicates(243) + confusable_with(18)
```

**编码警告**：utterances CSV 是 **GBK**，用 pandas 读时加 `encoding="gbk"`。knowledge_graph 的 CSV 是 UTF-8 BOM。v1.2 JSON 是 UTF-8。

---

## 五、必须避免的坑

### 🔴 绝对不要踩

1. **不要用文件名的含义判断内容。** `fraud_utterances.csv` 当前 label=fraud（诈骗数据），`ad_utterances.csv` 当前 label=ad（广告数据）。但之前发生过文件名与内容互换的事故。**训练时永远按 label 字段读，不要按文件名猜。**

2. **不要改 CSV 的编码。** utterance CSV 是 GBK。用 UTF-8 打开会出现乱码。如果你看到乱码，那是你的编辑器编码不对，不是文件坏了。

3. **不要改 split 目录。** splits 目录由 `split_data.py` 在本地生成。

4. **不要新增脚本文件。** 用户明确要求"不要新增文件，要在原代码基础上改"。所有功能在现有 6 个 .py 文件里完成。如果觉得需要新脚本，先回退到现有代码。

5. **广告话术要有活人感。** 广告话术不要写成套话模板。"先生您好，请问您是XX吗"这种用多了就是模板，要补充回访型、续保型、老客户跟进型话术。

6. **注意广告和正常的灰色地带。** 课后回访、售后回访、社区公共服务通知 = 正常通话，不是广告。广告的核心特征是**主动推销、喊人花钱**。拿不准的来找我确认。

7. **Linter 可能会修改 pattern JSON 文件中的字段名。** 如果 pattern JSON 看起来被改动了（字段名变了、格式变了），那是 VS Code 的某个 JSON 扩展自动做的美化/修复，不是用户手动改的。别怀疑用户，直接按当前文件内容继续工作就行。

### 🟡 注意

8. **用户名是 Subway，Windows 路径是 `C:\Users\Subway\Desktop\诈骗电话识别`。** VM 里映射为 `/sessions/<id>/mnt/诈骗电话识别/`。brabus 文件操作可以直接通过 Read/Write/Edit 工具读写 Windows 路径。

9. **用户是校赛选手，不是专业开发者。** 解释要直白，别用技术黑话。被骂了不要怼回去，直接承认错误并重做。

10. **跨场景数据收集不等于凭空乱写。** 每条新话术要基于真实来源（反诈通报/警方话术清单/真实录音），不能自己拍脑袋编。拿不准要多方验证。

---

## 六、下一步谁该做什么

### 成员 A（算法数据）—— 你在这个会话的位置

7.30 数据工作已全部完成。知识图谱也前置建好了。接下来的优先级：

1. ✅ **数据工作** — 已完成
2. ✅ **知识图谱构建** — 已前置完成（v1.2 pattern JSON + entities.csv + relations.csv）。**拿样本去跟成员B当面确认格式。**
3. **8.4-8.5 话术模板匹配算法**：基于三档权重写匹配打分函数。数据源：`knowledge_graph/relations.csv`（indicates 关系）。
4. **8.6-8.7 难例挖掘**：**前置条件：成员B模型已出第一批错误分类结果。** 不满足就跳过。
5. **8.8-8.9 知识图谱与模型联动**：先跟成员B聊清楚接口再动手。知识图谱已就绪，可以当场给他讲怎么用。
6. **8.10 数据迭代**：根据模型反馈补充数据。

### 知识图谱给成员B看什么

带这两个文件去给成员B看：
- `knowledge_graph/entities.csv` — 词典。问："这两类实体（ScamType / Entity）够不够、还需要什么维度？"
- `knowledge_graph/relations.csv` — 连线。问："这三种关系你能直接用吗？格式要不要调整？"

确认后如果格式要改，趁我在的时候当场改掉。

### ⚠️ 对计划表的修正建议

这份计划表是AI生成的，以下问题需要注意：
- 日期编号全部是乱的（8.10/8.20/8.30 应该是 8.1/8.2/8.3）
- 知识图谱构建两天太短，如果发现时间不够，可以降级为"导出+补全"
- 难例挖掘的前置条件是模型先出结果，没满足就跳过
- 模板匹配算法和知识图谱最好合并安排，不要分两段做
- 没有写每个人的分工和前后置条件，会上需要对齐

---

## 七、灰色地带判断经验

经过本次会话大量灰色地带审查，总结以下判断逻辑：

### 广告 vs 正常通话

| 场景 | 判据 | 举例 |
|------|------|------|
| 老客户回访+产品质量询问 | ✅ 正常 | "您之前买的血压计用着还顺手不，有没有需要校准的地方" |
| 老客户回访+推荐新产品/促销 | ✅ 广告 | "您之前买的血压计该换了，我们新到了一款升级版打八折" |
| 已有服务关系+提醒续费/到期 | ✅ 正常 | "您的保单下个月就到期了，提醒您一下" |
| 已有服务关系+推荐其他产品 | ✅ 广告 | "您的车险快到期了，顺便推荐您了解一下我们的重疾险" |
| 社区公共服务通知 | ✅ 正常 | "社区医院最近在做老年人免费体检" |
| 商业机构免费活动宣传 | ✅ 广告 | "我们新开了免费体验课，欢迎带孩子来试试" |

### 诈骗 vs 正常通话

| 场景 | 判据 | 举例 |
|------|------|------|
| 通知欠费/到期但无威胁无链接 | ✅ 正常 | "您的宽带欠费需要续费" |
| 通知欠费+附带链接/要求转账 | ✅ 诈骗 | "您的宽带欠费，点击链接续费否则停机" |
| 说你是幸运用户但免费送礼 | ✅ 诈骗 | "您被选中幸运客户，获得免费送礼机会"（后面会要运费/保证金） |

**核心原则：看有没有要你掏钱、提供信息、点链接。没有这些就是正常通知。**
